# Planned next steps

- Export buttons should open destination chooser for user instead of saving the 
plan in same destination folder
- Add the incoming-record model so the plan is no longer expense-only
- Continue Phase 7 with shared cashflow aggregation and income category summaries
